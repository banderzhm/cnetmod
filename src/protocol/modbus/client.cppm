/// cnetmod.protocol.modbus:client — Modbus Client Aggregation
/// Exports all client types: TCP, UDP, RTU

export module cnetmod.protocol.modbus:client;

export import :tcp_client;
export import :udp_client;
export import :rtu_client;

