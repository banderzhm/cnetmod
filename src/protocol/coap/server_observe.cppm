/// cnetmod.protocol.coap:server_observe - server Observe implementation partition.

module;

#include <cnetmod/config.hpp>

export module cnetmod.protocol.coap:server_observe;

export import :server;
