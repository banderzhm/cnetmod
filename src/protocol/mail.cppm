/// cnetmod.protocol.mail -- SMTP client and server protocol aggregation.

export module cnetmod.protocol.mail;

export import :types;
export import :codec;
export import :client;
export import :server;
