/// cnetmod.protocol.coap:server_lifecycle - server construction, listening, and run loop implementation partition.

module;

#include <cnetmod/config.hpp>

export module cnetmod.protocol.coap:server_lifecycle;

export import :server;
