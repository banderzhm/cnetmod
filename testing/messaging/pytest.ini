[pytest]
asyncio_mode = auto
testpaths = amqp091 amqp10 kafka
markers =
    interoperability: requires Docker and a cnetmod interoperability driver
    stability: performs reconnect, restart, or sustained-load checks
    security: requires a TLS/SASL endpoint supplied by the environment
    slow: takes longer than an ordinary unit test
