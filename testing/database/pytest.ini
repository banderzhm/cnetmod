[pytest]
addopts = -ra
testpaths = postgresql mongodb
markers =
    postgresql: requires CNETMOD_POSTGRESQL_URI
    mongodb: requires CNETMOD_MONGODB_URI
    cnetmod_driver: invokes a compiled cnetmod JSON-lines driver
