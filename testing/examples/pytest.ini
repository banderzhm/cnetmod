[pytest]
addopts = -ra
testpaths = database
markers =
    postgresql_example: PostgreSQL production example end-to-end test
    mongodb_example: MongoDB production example end-to-end test
    destructive: intentionally interrupts a database primary or endpoint
